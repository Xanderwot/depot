<%= semantic_form_for [@product, @message], :remote => true do |f| %>
  	<%= f.hidden_field :parent_id %>
  	<p>
    	<%= f.label :content, "New Message" %><br />
    	<%= f.text_area :content, :rows => 4, :value => "" %>
  	</p>
  <p><%= f.submit "Post Message" %></p>
<% end %>